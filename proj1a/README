Orderqueue class: implements my queue which is a singly linked list of OrderNode structs, relies on
the provided Order class that implements the Order struct

instance variables: start pointer to the first OrderNode in the list, tail pointer to the end
of the list

Nozama class: contains the main function that simulates the warehouse and prints the output file
info to cout, uses the provided Parser class to read the input file, keeps track of time with a
function that increments time and performs the necessary actions (moving Orders around and editing
their information within the fetcher and packer arrays), the increment_time function will be called
repeatedly in a while loop in the main function 

instance variables: int clock, Queue inputOrders, Queue [] fetchers, Queue[] packers, constructor
takes in a number of packers, an input filename, and an algorithim type
